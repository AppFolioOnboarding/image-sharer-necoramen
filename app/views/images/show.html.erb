<h1>Showing image <%= @image.id %></h1>

<span>Tags:</span>
<% if @image.tags.empty? %>
  <p>No tag is available for this image.</p>
<% else %>
  <% @image.tags.each do |tag| %>
    <span class="image-tag">#<%= tag %></span>
  <% end %>
<% end %>
<br>

<img src="<%= @image.url%>" width="600"/>
